RULES REGARDING PREFABS

Prefabs are used to store a mother or original Objects that needs or can be clone, 
do not put objects that you won't be cloning later in the Prefabs

The things that are stored in the prefabs are only Unity objects